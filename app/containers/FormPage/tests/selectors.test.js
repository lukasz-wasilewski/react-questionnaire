// import { fromJS } from 'immutable';
// import { makeSelectFormPageDomain } from '../selectors';

// const selector = makeSelectFormPageDomain();

describe('makeSelectFormPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
